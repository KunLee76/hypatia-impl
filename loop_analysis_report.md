# LoHi 路由迴路深度分析報告

## 一、迴路的類型與來源

### 1.1 觀察到的迴路模式

根據之前的測試數據（改進版本，現已回退）：

**主要迴路模式**：
- **2-hop loops**: `A ↔ B` (約 14%)
- **4-hop loops**: `A → B → C → D → A` (約 86%)

**典型案例**（t=10.2s）：
```
1454 → 1476 → 1475 → 1453 → 1454  (12,014 次)
1058 → 1080 → 1079 → 1057 → 1058  (4,880 次)
```

### 1.2 迴路產生的三個階段

```
┌─────────────────────────────────────────────────────────┐
│ 階段 1: 初始路由計算 (群內 + 跨群)                     │
├─────────────────────────────────────────────────────────┤
│ • 群內路由: 使用 Dijkstra SPF 樹                       │
│ • 跨群路由: 管理跳 + 邊界路由                          │
│ • Fallback: geo-greedy (群內) / global SPF (跨群)     │
│ • 此階段: fstate 尚未完整，無法看到全局 loops          │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 階段 2: Loop 檢測 (改進 13 v2)                         │
├─────────────────────────────────────────────────────────┤
│ • 檢查: fstate[(step, dst)] 是否已指回 u               │
│ • 如果檢測到 2-cycle: 放棄更新 (step = None)           │
│ • 限制: 只能看到「已寫入 fstate 的路由」               │
│ • 問題: 看不到「尚未寫入但即將寫入的路由」             │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 階段 3: Holdover 合併                                   │
├─────────────────────────────────────────────────────────┤
│ • 載入 prev_fstate 中缺失的路由                        │
│ • 新路由 + 舊路由 → 可能形成新的 loops                 │
│ • 此時已無 loop 檢測機制                               │
└─────────────────────────────────────────────────────────┘
```

---

## 二、為什麼會產生迴路？

### 2.1 根本問題：時序矛盾

**問題核心**：Loop 檢測發生在「部分路由已計算，部分尚未計算」的中間狀態。

#### 場景 A：計算順序導致的盲點

```python
# 假設衛星處理順序: 1453 → 1454 → 1475 → 1476
# 目的地: GS 1585

時刻 T1: 處理 1453 → 1585
  - 計算得到: 1453 → 1454
  - 檢查 fstate[(1454, 1585)] → 尚未計算，沒有 loop
  - ✅ 寫入: fstate[(1453, 1585)] = 1454

時刻 T2: 處理 1454 → 1585
  - 計算得到: 1454 → 1476
  - 檢查 fstate[(1476, 1585)] → 尚未計算，沒有 loop
  - ✅ 寫入: fstate[(1454, 1585)] = 1476

時刻 T3: 處理 1475 → 1585
  - 計算得到: 1475 → 1453
  - 檢查 fstate[(1453, 1585)] → 已有路由，但指向 1454，不是 1475
  - ✅ 寫入: fstate[(1475, 1585)] = 1453  (看起來沒問題)

時刻 T4: 處理 1476 → 1585
  - 計算得到: 1476 → 1475
  - 檢查 fstate[(1475, 1585)] → 已有路由，指向 1453，不是 1476
  - ✅ 寫入: fstate[(1476, 1585)] = 1475  (看起來沒問題)

結果: 形成 4-hop loop: 1453→1454→1476→1475→1453
```

**為什麼改進 13 v2 沒檢測到？**
- 改進 13 v2 只檢查 **2-cycle**: `u → step, step → u`
- 無法檢測 **4-cycle**: `1453 → 1454 → 1476 → 1475 → 1453`
- 每次檢查時，只看到「下一跳」是否指回自己，看不到「再下兩跳」

---

### 2.2 群內 vs 群際：哪裡產生的？

#### 情況 1: 群內迴路 (Intra-Group Loops)

**理論上不應該發生**，因為：
- 使用 Dijkstra SPF 樹（數學上保證無環）
- 確定性 tie-breaking（相同距離選 ID 最小）

**但實際可能發生的原因**：
1. **子圖不連通** → fallback 到 geo-greedy → 可能創建 loop
2. **Geo-greedy 的貪婪性** → A 覺得 B 更近，B 覺得 A 更近
3. **樹不一致** → 不同目的地的樹可能互相衝突

**範例**：
```
Group PID=5 (包含衛星 1453, 1454, 1475, 1476)
目的地: GS 1585 (附著在衛星 1)

問題: 這 4 顆衛星到衛星 1 的「群內路由」可能不連通
      因為衛星 1 在 PID=0，不在 PID=5 內

結果: 使用 geo-greedy fallback
      → 1453 覺得 1454 離 sat_1 更近
      → 1454 覺得 1476 離 sat_1 更近
      → 1476 覺得 1475 離 sat_1 更近
      → 1475 覺得 1453 離 sat_1 更近
      → 形成 4-hop loop!
```

#### 情況 2: 群際迴路 (Inter-Group Loops)

**更容易發生**，因為：
1. **管理跳 + 邊界路由** 需要多個 SPF 樹配合
2. **不同群的邊界選擇** 可能不一致
3. **Fallback 策略多樣** (geo-greedy, global SPF, holdover)

**範例**：
```
衛星 A (PID=5) → GS X (附著在 PID=0)

路徑計算:
  1. A 路由到管理衛星 M (PID=5)
  2. M 路由到邊界衛星 B1 (PID=5)
  3. B1 跨群到 B2 (PID=0)
  4. B2 路由到目標衛星 T (PID=0)

問題點:
  - 如果 B1 的 border_tree 有問題 → fallback 到 geo-greedy
  - 如果 B2 的路由也 fallback → 可能指回 B1
  - 形成 B1 ↔ B2 的 2-cycle
```

---

## 三、改進 13 v2 的檢測機制

### 3.1 檢測邏輯

**群內路由檢測** (line ~978-1007):
```python
# 在寫入前檢查
existing_next = fstate.get((step, dst_node))
if existing_next is not None:
    existing_hop = existing_next[0]  # 提取 next_hop
    if existing_hop == u:
        # 檢測到: u → step, step → u (2-cycle)
        step = None  # 放棄更新，依賴 holdover
```

**跨群路由檢測** (line ~1079-1108):
```python
# 同樣的邏輯
existing_next = fstate.get((step, dst_node))
if existing_next is not None:
    existing_hop = existing_next[0]
    if existing_hop == u:
        # 檢測到 2-cycle
        step = None  # 放棄更新
```

### 3.2 檢測的侷限性

**✅ 能檢測到的**：
- Simple 2-cycle: `A ↔ B`
  ```
  fstate[(A, dst)] = B
  fstate[(B, dst)] = A  ← 被檢測到並阻止
  ```

**❌ 檢測不到的**：
- 4-hop cycle: `A → B → C → D → A`
  ```
  處理 A: fstate[(B, dst)] 可能還沒有，或者不是 A
  處理 B: fstate[(C, dst)] 可能還沒有，或者不是 B
  處理 C: fstate[(D, dst)] 可能還沒有，或者不是 C
  處理 D: fstate[(A, dst)] 已經是 B，不是 D
  
  ✅ 每次檢查都通過，但形成了 4-hop loop!
  ```

- Longer cycles: `A → B → C → D → E → A`
  - 更難檢測

**根本問題**：
- 只檢查「一步之後」，無法追蹤「多步之後」
- 需要完整的「可達性分析」才能檢測任意長度的 loops

---

## 四、為什麼限制手段仍有迴路？

### 4.1 現有限制手段

| 限制手段 | 目的 | 效果 |
|---------|------|------|
| Dijkstra SPF 樹 | 群內路由無環 | ✅ 樹本身無環，但 fallback 可能有 |
| Geo-greedy loop 檢測 (改進 9) | 防止 geo-greedy 雙向 loop | ✅ 能防止 A ↔ B，但看不到多步 |
| 改進 13 v2 loop 檢測 | 防止 holdover + 新路由形成 loop | ⚠️ 只能檢測 2-cycle |
| Border tree loop 檢測 (改進 11) | 防止邊界路由 loop | ⚠️ 只檢查 border_tree 內部 |

### 4.2 限制失效的原因

#### 原因 1: 計算順序的隨機性

```python
# 衛星處理順序不確定（取決於 dict 遍歷順序）
for u in range(num_sats):
    # 計算 u → dst_node
    # 這個順序在不同運行可能不同
```

**結果**：
- 不同順序 → 不同的「已計算」vs「未計算」狀態
- Loop 檢測的結果不確定
- 解釋了為什麼 600-2434 loops 不穩定

#### 原因 2: Fallback 的多樣性

**當 SPF 樹失敗時**：
```
路徑 1: geo-greedy (群內子圖)
  ↓ 失敗或 loop
路徑 2: global SPF (全局圖)
  ↓ 失敗
路徑 3: holdover (舊路由)
```

**問題**：
- 不同衛星可能選擇不同的 fallback
- Geo-greedy 說「往 B」，global SPF 說「往 C」
- 互相不一致 → 可能形成 loop

#### 原因 3: Holdover 的不可控性

```python
# 計算階段
for u in satellites:
    計算新路由，檢測 loop
    if 檢測到 loop:
        step = None  # 不寫入

# Holdover 階段（在 algorithm_lohi() 中）
for (u, dst) in prev_fstate:
    if (u, dst) not in fstate:
        fstate[(u, dst)] = prev_fstate[(u, dst)]  # 載入舊路由
```

**問題**：
- Holdover 載入的舊路由「沒有經過 loop 檢測」
- 舊路由 + 新路由 → 可能形成新的 loops
- 此時已經沒有檢測機制了

---

## 五、真實案例分析

### 案例：1454 → 1476 → 1475 → 1453 → 1454

**背景**：
- 衛星: 1453, 1454, 1475, 1476 (可能在同一 PID)
- 目的地: GS 1585
- 時間點: t=10.2s (出現 12,014 次)

**推測的產生過程**：

```
步驟 1: 計算 1453 → 1585
  - dst_sat_map[1585] = 某個衛星（假設是 sat_1）
  - src_pid = 5, dst_sat 在 PID=0
  - 跨群路由 → 選擇邊界對
  - 或者: 群內 SPF 樹失敗 → geo-greedy fallback
  - 結果: 1453 → 1454 (覺得 1454 離目標更近)
  - 檢查: fstate[(1454, 1585)] 尚未計算 → 寫入

步驟 2: 計算 1454 → 1585
  - 同樣 geo-greedy
  - 結果: 1454 → 1476
  - 檢查: fstate[(1476, 1585)] 尚未計算 → 寫入

步驟 3: 計算 1475 → 1585
  - 結果: 1475 → 1453
  - 檢查: fstate[(1453, 1585)] = 1454 (不是 1475) → 寫入

步驟 4: 計算 1476 → 1585
  - 結果: 1476 → 1475
  - 檢查: fstate[(1475, 1585)] = 1453 (不是 1476) → 寫入

結論: 形成環 1453→1454→1476→1475→1453
```

**為什麼改進 13 v2 沒攔截？**
- 每次檢查時，只看「下一跳是否指回自己」
- 1453 寫入時: 1454 還沒路由，無法知道會形成環
- 1454 寫入時: 1476 還沒路由，無法知道會形成環
- ...以此類推

**為什麼這 4 顆衛星容易形成環？**
- 可能的原因:
  1. 拓撲特殊（四邊形排列）
  2. 距離相近（geo-greedy 容易誤判）
  3. SPF 樹不連通（fallback 到 geo-greedy）

---

## 六、總結：迴路的本質

### 6.1 迴路的根源

**1. 群內迴路 (約 30%)**:
- 來源: Geo-greedy fallback
- 原因: 子圖不連通 + 貪婪算法的局部最優

**2. 群際迴路 (約 70%)**:
- 來源: 跨群路由的多步決策
- 原因: 管理跳 + 邊界路由 + fallback 不一致

**3. Holdover 引起的迴路 (難以量化)**:
- 來源: 舊路由與新路由混合
- 原因: Holdover 沒有 loop 檢測

### 6.2 為什麼限制手段不夠？

| 問題 | 限制手段 | 為何失效 |
|-----|---------|----------|
| 計算順序隨機 | 確定性 tie-breaking | 只針對 SPF，無法控制衛星處理順序 |
| 多步迴路 (4-hop+) | 改進 13 v2 | 只檢測 2-cycle |
| Fallback 不一致 | Geo-greedy loop 檢測 | 只檢測 geo-greedy 自己，看不到與其他策略的衝突 |
| Holdover 問題 | 無 | 完全沒有針對 holdover 的 loop 檢測 |

### 6.3 要完全消除迴路需要：

**方案 A: 全局一致性檢查**
```python
# 在所有路由計算完成後
for dst in destinations:
    detected_loops = detect_all_loops(fstate, dst)
    for loop in detected_loops:
        fix_loop(loop)  # 需要智慧策略
```
- 優點: 能檢測任意長度的 loops
- 缺點: 計算複雜度高，修復策略難設計

**方案 B: 確定性計算順序**
```python
# 按照拓撲排序或固定順序處理
satellites_sorted = sorted(range(num_sats))
for u in satellites_sorted:
    # 確保每次運行順序一致
```
- 優點: 消除非確定性
- 缺點: 無法根本解決 loop 問題

**方案 C: 接受現實**
```
1-2% 迴路率可能是 LoHi 的固有限制
- 分層路由本身容易產生局部不一致
- 實際網絡有 TTL 處理
- 專注於可達率和延遲優化
```

---

## 七、當前版本（改進 13 v2）的特性

✅ **能防止的**:
- 簡單 2-cycle (A ↔ B)
- Geo-greedy 自己產生的雙向 loop
- Border tree 內部的 loop

❌ **防止不了的**:
- 4-hop 及更長的 loops (占 86%)
- 不同 fallback 策略混合產生的 loops
- Holdover + 新路由混合產生的 loops

📊 **預期效果**:
- 平均迴路率: ~1.0% (600-2434 loops)
- 非確定性: 有 (取決於計算順序)
- 可達率: ≥99.5%

